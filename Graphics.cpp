#include "Graphics.h"

static std::vector<std::vector<int>> returnMatrice(std::vector<int> input) {
    std::vector<std::vector<int>> tempVec;

    for(int i = 0; i < input.size(); i++) {
        int height = input[i];
        while(height != 0) {


            height--;
        }


    }

}
